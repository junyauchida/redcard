	<h1><%= @cardlist.cardlist_name %></h1>

<table class="table table-hover">
	<thead>
		<tr>
			<th>表</th>
			<th>裏</th>
		</tr>
	</thead>
	<tbody>
		<% @cardspage.each do |card|%>
		<tr>
			<td class="show-front"><%= simple_format(card.front) %></td>

			<td class="back">
				<div class="div-action">
				<%= simple_format(card.back) %>
				</div>
			</td>

		</tr>
		<%end%>
	</tbody>
</table>

<%= paginate @cardspage, class: "pagenate" %>

<% if @cardlist.user.id == current_user.id %>
	<p><%= link_to edit_cardlist_path(@cardlist.id), class: 'btn btn-default btn-lg btn-block' do %>
	<i class="fa fa-arrow-left"></i>
	カードリスト編集
	<% end %>
	</p>
<% else %>
<% end %>
<!-- <script>
	$(function(){
	$('.div-action').on('click', function()  {
	// $(this).toggleClass('background-color');

	if ($(this).hasClass('background-color')) {
		$(this).removeClass('background-color');
	} else {
		$(this).addClass('background-color');
	}

	});



    var $header = $('#top-head');
    // Nav Fixed
    $(window).scroll(function() {
        if ($(window).scrollTop() > 100) {
            $header.addClass('fixed');
        } else {
            $header.removeClass('fixed');
        }
    });
    // Nav Toggle Button
    $('#nav-toggle').click(function(){
        $header.toggleClass('open');
    });

});
</script> -->

